{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Show in console",
            "type": "shell",
            "linux": {
                "command": "/usr/bin/node ${file}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}